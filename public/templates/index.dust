{>"layouts/master" /}

{<body}
    {#books}
    	<div class="large-3 columns book end">
    		<img src="{.cover}">
    		<h4>{.title}</h4>
    		<p>{.truncText}</p>
    		<div class="price">ราคา<span>฿{.price}</span></div>
    		<br>
    		<a href="/books/details/{._id}" class="button small">รายละเอียดหนังสือ</a>
    	</div>
    {/books}
{/body}
